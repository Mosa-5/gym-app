/** Shared magic numbers used across the app. */

/** Product price-filter bounds, in USD. */
export const PRICE_MIN = 0;
export const PRICE_MAX = 1000;

/** Review comment length bounds, in characters. */
export const REVIEW_MIN_LENGTH = 40;
export const REVIEW_MAX_LENGTH = 600;
